# NOTE: Implementation to be generated by the agent.
FROM eclipse-temurin:21-jre

WORKDIR /app
# The agent will COPY built artifacts into /app and set up an entrypoint:
# COPY target/... /app/
# ENTRYPOINT ["java","-jar","/app/apidefender.jar","scan"]

# Keep this file as a scaffold; the agent must finalize it.
